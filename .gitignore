# ── R session artifacts ───────────────────────────────────────────────────────
.Rhistory
.RData
.Rproj.user/
*.Rproj

# ── knitr / R Markdown cache ──────────────────────────────────────────────────
*_cache/
*_files/
*.html          # rendered HTML outputs (reproducible from .Rmd — no need to track)

# ── Pipeline intermediate outputs ─────────────────────────────────────────────
# These are generated by running the scripts and should not be committed.
# Keep the example_data/ folder tracked (it IS source, not output).
01_data_cleaning/compounds_ready.csv
02_adme_filtering/compounds_filtered.csv
02_adme_filtering/for_swissadme.txt
03_toxicity_filtering/for_admetlab.csv
03_toxicity_filtering/compounds_safe.csv
04_target_prediction/df_targets.csv

# ── Step 05 outputs ───────────────────────────────────────────────────────────
05_network_analysis/output/
# Exception: track the edge list so the network is reproducible without STRING
!05_network_analysis/output/PPI_EdgeList.csv
!05_network_analysis/output/network_final.csv

# ── Step 06 outputs ───────────────────────────────────────────────────────────
06_predocking_analysis/output/
# Exception: track the final candidate table — it is the key deliverable
!06_predocking_analysis/output/Docking_Candidates_Ranked.csv
!06_predocking_analysis/output/DrugGene_Validation.csv

# ── Large binary files (TIFFs, PDFs, PNGs) ───────────────────────────────────
*.tiff
*.pdf
# Keep example PDFs if any

# ── OS junk ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Real project data (never commit patient/unpublished data) ─────────────────
# Uncomment and adapt if you add your own raw data files:
# my_project/raw/
# my_project/data/
